'use strict'

import { CHIPS_WAP_BASE_URL } from '../config/constant'

const userApi = {
  updateAddress: `${CHIPS_WAP_BASE_URL}/yk/address/v1/add_shipping_address.do`, // 首页首屏服务端初始化请求数据
}
export { userApi }
